# nRPC AI Service Configuration (Optional Defaults)
# These variables provide default values for the AI translation features powered by nRPC
# Users can configure their own nRPC server in the AI Settings UI, which will override these defaults

# Default public key of the nRPC server (hex or npub format)
# Example: npub1... or hex pubkey
# Leave empty to require users to configure in AI Settings
REACT_APP_NRPC_SERVER_PUBKEY=

# Default comma-separated list of Nostr relays to use for nRPC communication
# Example: wss://relay.damus.io,wss://relay.snort.social
REACT_APP_NRPC_RELAYS=wss://relay.damus.io,wss://relay.snort.social

# Note: Users configure their nRPC server pubkey and relays in the AI Settings screen.
# These environment variables are only used as fallback defaults.
